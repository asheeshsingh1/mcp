[project]
name = "my-mcp-server"
version = "0.1.0"
description = "MCP server"
readme = "README.md"
authors = [
    { name = "Asheesh Singh", email = "asheeshsingh0112@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastmcp>=2.3.0",
]

[project.scripts]
my-mcp-server = "my_mcp_server:main"
